import * as _LocalStorage from './local-storage';
import * as _SessionStorage from './session-storage';

export namespace Models {

    export import LocalStorage = _LocalStorage.LocalStorage;
    export import SessionStorage = _SessionStorage.SessionStorage;

}
